---
description: >-
  No-frontmatter sequence diagram with `Note over` statements placed on a
  plain `actor` and on a `boundary`-typed participant, checking that note
  placement works correctly against both shapes.
---
sequenceDiagram
  actor Alice
  participant Charlie@{ "type" : "boundary" }
  note over Alice: Some note
  note over Charlie: Other note
