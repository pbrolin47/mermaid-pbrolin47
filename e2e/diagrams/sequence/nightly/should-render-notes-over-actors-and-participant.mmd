---
description: >-
  No-frontmatter sequence diagram with `Note over` statements placed on a
  plain `actor` and on a plain (untyped) `participant`, checking that note
  placement works correctly against both default shapes. Unlike the
  sibling "-and-boundary" fixture, this one uses an ordinary participant
  rather than a typed `boundary` shape.
---
sequenceDiagram
  actor Alice
  participant Charlie
  note over Alice: some note
  note over Charlie: other note
