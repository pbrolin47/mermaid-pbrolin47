description: >
  Cross-boundary subgraph layout under elk — three subgraphs (Frontend,
  Backend, Auth) where Frontend has an edge crossing into each of the other
  two. Mirrors 2-layout-dagre-subgraphs.mmd's structure exactly so the two
  only differ by layout engine, not by diagram shape. Composite/cross-boundary
  edges are a known-risky area for elk (see e2e/folder-based-screenshots.md),
  so this combination stays in the pr tier deliberately.
tags: [layout, elk, subgraphs]
