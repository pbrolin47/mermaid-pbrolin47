description: >
  Cross-boundary subgraph layout under dagre — two subgraphs (Frontend,
  Backend) with an edge crossing between them. Mirrors
  3-layout-elk-subgraphs.mmd's structure exactly so the two only differ by
  layout engine, not by diagram shape.
tags: [layout, dagre, subgraphs]
