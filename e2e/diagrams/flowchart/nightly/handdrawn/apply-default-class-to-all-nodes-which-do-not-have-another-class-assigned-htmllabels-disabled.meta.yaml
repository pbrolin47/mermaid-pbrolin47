description: "Verifies that a classDef named default is automatically applied to every node without an explicit class assignment, while nodes with explicit classes keep their own styling, with HTML labels disabled, under the hand-drawn look."
tags:
  - "nightly"
  - "copied-from-catalogue"
  - "handdrawn"
