description: "Verifies that a node first introduced via an edge inside a subgraph (rather than inside a nested child subgraph) is treated as a sibling member of that outer subgraph, with dagre correctly laying out an edge from a deeply nested child to that sibling."
tags:
  - "nightly"
  - "copied-from-catalogue"
  - "dagre"
