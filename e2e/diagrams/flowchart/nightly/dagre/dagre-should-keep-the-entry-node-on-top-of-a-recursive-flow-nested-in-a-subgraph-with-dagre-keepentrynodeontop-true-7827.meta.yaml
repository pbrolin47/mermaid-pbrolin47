description: "Regression test (issue 7827) verifying that when a recursive/cyclic flow (brief -> ... -> decision -> brief) is nested inside a subgraph, with keepEntryNodeOnTop enabled dagre keeps the subgraph's true entry node positioned at the top rather than letting the back edge pull it out of place."
tags:
  - 'nightly'
  - 'copied-from-catalogue'
  - 'dagre'
