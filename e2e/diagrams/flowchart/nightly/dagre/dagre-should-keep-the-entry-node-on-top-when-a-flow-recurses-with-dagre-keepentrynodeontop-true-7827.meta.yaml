description: "Regression test (issue 7827) for a flat cyclic flow (brief -> ... -> decision -> brief) verifying that with keepEntryNodeOnTop enabled, dagre keeps the entry node at the top of the layout despite the back edge feeding into it from the decision node."
tags:
  - "nightly"
  - "copied-from-catalogue"
  - "dagre"
