description: "Regression test for a node that is referenced inside one subgraph's internal edge and also explicitly declared as a member of a different subgraph, verifying dagre assigns it consistently to a single subgraph."
tags:
  - 'nightly'
  - 'copied-from-catalogue'
  - 'dagre'
