description: "Verifies the same unsupported-markdown/raw-string label handling (literal comparisons, markdown-styled subgraph titles, list syntax in backtick labels, etc.) still renders correctly as plain SVG text when htmlLabels is set to false instead of the default true."
tags:
  - "nightly"
  - "converted-from-rendering-catalogue"
  - "v2"
