description: "Renders two sibling subgraphs (One, Two) under the redux-color theme where a classDef recolors a node inside One and an inline style directive recolors the Two container itself, checking that explicit user styling overrides the theme's automatic subgraph coloring."
tags:
  - 'nightly'
  - 'converted-from-rendering-catalogue'
  - 'redux-color-subgraphs'
