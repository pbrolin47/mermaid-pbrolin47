description: 'Verifies that edges can be annotated with @{ animation: slow|fast } metadata to render animated dashed-line edges, alongside independent node styling via style directives.'
tags:
  - 'nightly'
  - 'converted-from-rendering-catalogue'
  - 'classic'
