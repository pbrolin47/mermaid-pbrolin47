description: "Verifies that a classDef defining a white text color (color:#fff) alongside fill/stroke applies correctly when selectively assigned via `class` to two of three nodes in a chain, leaving the middle node unstyled."
tags:
  - "release"
