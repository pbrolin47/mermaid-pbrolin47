description: "Verifies that, under the ELK layout engine, a subgraph's title text width is reserved before edge routing when the title is wider than every child node it contains, so that edges terminate at the subgraph's painted border rather than ending inside it, and the frame is not resized after the fact."
tags:
  - "release"
