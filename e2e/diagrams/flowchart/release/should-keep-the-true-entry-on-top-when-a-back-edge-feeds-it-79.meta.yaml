description: "Verifies that the entry node of a cyclic flow (stockholm, which is fed by a back-edge from end_decision when the answer is 'No') stays pinned to the top of the layout via the elk.keepEntryNodeOnTop option, using the elk layout engine under which this option is actually implemented."
tags:
  - 'release'
