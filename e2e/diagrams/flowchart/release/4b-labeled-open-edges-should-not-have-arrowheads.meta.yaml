description: 'Verifies that open edges drawn with the plain-line syntax (---), whether unlabeled, given an inline label, or given a `-- text --` label, render without arrowheads, in contrast to a bidirectional edge (<-- ... -->) which does show arrowheads on both ends.'
tags:
  - 'release'
