description: 'Verifies that a subgraph annotated with the @{ view: collapsed } shape-data directive renders as a single collapsed node rather than expanding its children, and that edges which originally pointed into or out of the subgraph are redirected to that collapsed node.'
tags:
  - 'release'
