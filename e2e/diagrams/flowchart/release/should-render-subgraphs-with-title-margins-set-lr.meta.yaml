description: "Verifies that the subGraphTitleMargin config renders correctly for a left-to-right flowchart containing nested subgraphs with mixed inner directions (TB, RL, BT), using the default htmlLabels rendering."
tags:
  - "release"
