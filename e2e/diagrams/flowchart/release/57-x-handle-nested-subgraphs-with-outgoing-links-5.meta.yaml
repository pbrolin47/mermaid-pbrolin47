description: "Documents a known-broken variant of the container_Alpha/container_Beta cross-subgraph edge case (see file 56) where simply reordering the two outgoing edges inside container_Alpha changes the rendering outcome; the source comment explicitly notes this ordering 'does not produce the desired result', capturing an edge-order-dependent layout regression."
tags:
  - "release"
