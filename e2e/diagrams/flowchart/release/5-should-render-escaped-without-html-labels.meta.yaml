description: 'Verifies HTML-tag escaping in a node label (<strong>Haiya</strong>) when htmlLabels is disabled at both the top-level and flowchart-specific config, using a longer arrow (---->) between the nodes.'
tags:
  - 'release'
