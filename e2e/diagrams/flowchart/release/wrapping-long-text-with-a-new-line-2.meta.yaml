description: "Verifies that a markdown string node label containing very long text, an inline bold/italic run, and an explicit blank-line paragraph break wraps correctly onto multiple lines, with htmlLabels disabled so wrapping is computed for SVG text rather than HTML."
tags:
  - "release"
