description: "Verifies that a decision-diamond node label containing multiple `<br/>` line breaks with long text per line renders correctly and wraps without distorting the diamond shape's layout."
tags:
  - 'release'
