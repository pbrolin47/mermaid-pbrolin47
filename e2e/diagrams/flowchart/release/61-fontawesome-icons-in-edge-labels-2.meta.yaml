description: "Verifies that FontAwesome icon shorthand syntax (fa:fa-car) renders correctly both inside an edge label and inside a node's own text label, using the legacy `graph` keyword."
tags:
  - "release"
