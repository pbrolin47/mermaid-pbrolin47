description: "Verifies that self-loop edges render correctly when the flowchart or an enclosing subgraph uses a non-default direction (LR, and subgraphs with direction BT and RL), ensuring the loop geometry adapts to each layout direction rather than only working in the default top-to-bottom flow."
tags:
  - "release"
