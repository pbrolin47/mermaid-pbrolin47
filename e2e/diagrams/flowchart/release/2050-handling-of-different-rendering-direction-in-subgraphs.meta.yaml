description: "Verifies that nested subgraphs can each declare their own `direction` (RL and BT) independently of their parent subgraph's direction (TB) and the top-level diagram's direction (LR), and that edges connecting into, out of, and between the subgraphs still render correctly."
tags:
  - "release"
