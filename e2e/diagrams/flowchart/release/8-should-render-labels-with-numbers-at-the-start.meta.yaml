description: "Checks that a node whose id/label is purely numeric ('1') parses and renders correctly inside a subgraph, using semicolon-terminated statement syntax (graph TB;subgraph ...;1;end;)."
tags:
  - 'release'
