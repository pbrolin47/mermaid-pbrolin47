description: "Checks that a large nodeSpacing/rankSpacing config value (250) is honored for layout of nodes inside subgraphs that declare their own direction (LR) nested within an LR flowchart."
tags:
  - "release"
