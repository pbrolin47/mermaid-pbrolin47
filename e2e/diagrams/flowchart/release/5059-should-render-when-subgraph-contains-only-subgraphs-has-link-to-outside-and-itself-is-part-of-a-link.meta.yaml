description: "Checks that a parent subgraph (Main) whose only members are two child subgraphs (Child1, Child2), with no direct child nodes of its own, can still have an outgoing edge from the parent container itself (Main --> Out1) at the same time one of its child subgraphs also has its own outgoing edge (Child2 --> Out2)."
tags:
  - "release"
