description: "Verifies that per-node `style` statements apply fill, stroke, stroke-width, text color, and stroke-dasharray to individual round-edged nodes, using the `flowchart` keyword variant of the same test as the '-2' fixture."
tags:
  - 'release'
