description: "Verifies that trapezoid ([/.../]) and inverse-trapezoid ([\\...\\]) node shapes render correctly, including a node with a long multi-line label wrapped via <br/> tags."
tags:
  - "release"
