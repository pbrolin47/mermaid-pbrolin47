description: "Verifies that style and linkStyle color declarations apply correctly across the original core set of node shapes (rectangle, round, stadium, subroutine, cylinder, circle, asymmetric, rhombus, hexagon) using the legacy `graph` keyword, a smaller shape set than the '-v2' fixture."
tags:
  - "release"
