description: 'Verifies that an edge originating from a subgraph/cluster (bsub-->a) still routes correctly when that cluster contains several levels of nested descendant subgraphs (bsub > csub > dsub, and bsub > esub > fsub > gsub), exercising cluster-edge routing amid deep, multi-branch subgraph nesting.'
tags:
  - 'release'
