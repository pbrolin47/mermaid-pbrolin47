description: 'Verifies three-level-deep subgraph nesting (O contains A, A contains nodes a/b plus subgraph B, B contains c), with edges that cross multiple nesting levels at once (b --> B and a --> c).'
tags:
  - 'release'
