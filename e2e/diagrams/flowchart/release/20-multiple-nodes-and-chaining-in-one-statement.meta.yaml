description: "Verifies the `-- label -->` edge-label syntax, an inline class-shorthand assignment (`:::exClass`), and the `&` multi-node operator combined with chaining, all within one semicolon-terminated statement (`A -- hello --> B:::exClass & C --> D;`)."
tags:
  - "release"
