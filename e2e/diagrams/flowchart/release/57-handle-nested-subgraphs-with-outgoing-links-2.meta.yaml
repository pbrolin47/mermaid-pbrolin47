description: "In a top-down flowchart with three sibling subgraphs (one, two, three), checks a mix of edges between subgraph containers themselves (one --> two, three --> two, two --> c2) and a cross-subgraph node-to-node edge (c1 --> a2) declared in the source before either subgraph is defined."
tags:
  - "release"
