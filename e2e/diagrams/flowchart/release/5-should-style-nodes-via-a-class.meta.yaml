description: 'Verifies that a single classDef with multiple CSS properties (fill, color, font-weight, stroke-width, stroke) can be applied to several nodes at once via one `class` statement, across a chain of node ids that include numeric-leading identifiers.'
tags:
  - 'release'
