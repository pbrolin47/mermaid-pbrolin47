description: 'Verifies that the `&` multi-node operator combined with chained arrows in a single statement (`a --> b & c --> d`) correctly produces four edges: a->b, a->c, b->d, and c->d.'
tags:
  - 'release'
