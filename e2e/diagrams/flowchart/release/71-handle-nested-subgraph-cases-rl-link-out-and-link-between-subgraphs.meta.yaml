description: 'Tests the same subgraph-to-subgraph link and cross-subgraph node link pattern as the analogous TB/BT fixtures (S1 --> S2 plus sub1 --> sub4), but with the flowchart set to RL (right-to-left) direction.'
tags:
  - 'release'
