description: 'Tests a flowchart in TB direction with both a link between two subgraph containers (S1 --> S2) and a direct link between individual nodes that belong to two different subgraphs (sub1 --> sub4), verifying edges correctly cross subgraph boundaries.'
tags:
  - 'release'
