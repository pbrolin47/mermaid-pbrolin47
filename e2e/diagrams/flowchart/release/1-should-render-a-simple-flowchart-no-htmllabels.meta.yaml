description: 'Verifies that a basic flowchart with square, round, and diamond decision nodes, edge labels, and a FontAwesome icon in a node label renders correctly when htmlLabels is explicitly disabled, forcing SVG text rendering instead of foreignObject HTML labels.'
tags:
  - 'release'
