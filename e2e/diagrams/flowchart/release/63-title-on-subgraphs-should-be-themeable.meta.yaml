description: "Verifies that subgraph title text color respects the theme's titleColor themeVariable when a custom base theme with darkMode enabled is set via a frontmatter %%{init}%% directive, across two connected subgraphs."
tags:
  - 'release'
