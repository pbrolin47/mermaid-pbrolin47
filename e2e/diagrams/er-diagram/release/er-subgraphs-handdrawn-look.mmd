---
description: Two entities grouped inside a `subgraph` block under the handDrawn look.
config:
  look: handDrawn
---
erDiagram
    subgraph customers
      CUSTOMER
      ORDER
    end

    CUSTOMER ||--o{ ORDER : places
