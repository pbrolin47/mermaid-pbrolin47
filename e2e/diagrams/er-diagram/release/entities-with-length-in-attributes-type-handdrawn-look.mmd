---
description: >-
  Attribute types carrying an explicit length parameter (varchar(99),
  string(255)) under the handDrawn look.
config:
  look: handDrawn
---
erDiagram
    CLUSTER {
      varchar(99) name
      string(255) description
    }
