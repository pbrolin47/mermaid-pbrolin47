---
description: >-
  Nested subgraphs (a "details" subgraph inside "domain") where the inner
  subgraph's `direction TB` overrides the outer diagram's `direction LR`, plus a
  relationship targeting the subgraph itself rather than an entity inside it
  under the handDrawn look.
config:
  look: handDrawn
---
erDiagram
    direction LR

    subgraph domain
      direction TB

      CUSTOMER

      subgraph details
        ORDER
      end
    end

    PRODUCT

    PRODUCT ||--o{ domain : links
    CUSTOMER ||--o{ ORDER : places
