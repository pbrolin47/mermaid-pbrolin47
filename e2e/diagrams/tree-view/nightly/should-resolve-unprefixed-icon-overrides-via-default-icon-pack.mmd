---
description: >-
  Tests that an unprefixed icon name in an `icon(...)` override (`bell`)
  resolves against the configured `defaultIconPack` (fa) rather than
  requiring an explicit pack prefix.
config:
  treeView:
    defaultIconPack: fa
---
treeView-beta
  src/
      alarm.txt icon(bell)
      index.js
