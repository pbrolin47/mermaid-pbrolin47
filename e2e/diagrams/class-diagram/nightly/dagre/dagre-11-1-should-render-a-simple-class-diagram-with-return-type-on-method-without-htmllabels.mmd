---
description: >-
  Generic class with an array-typed attribute and methods that take and
  return array types (e.g. `test(int[] ids) bool`, `testArray() bool[]`)
  under the dagre layout, rendered with `htmlLabels: false` to confirm
  array-type method signatures render correctly without HTML labels.
config:
  logLevel: 1
  htmlLabels: false
  layout: dagre
---
classDiagram
  class Class10~T~ {
  int[] id
  test(int[] ids) bool
  testArray() bool[]
  }
