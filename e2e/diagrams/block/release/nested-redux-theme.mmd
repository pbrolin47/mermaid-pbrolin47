---
description: >-
  Nested composite blocks (an "outer" container holding two inner containers,
  plus a top-level sibling) under the "redux" redux colour theme — verifies the
  palette applies at more than one nesting depth.
config:
  theme: redux
  look: neo
---
block-beta
  columns 1
  block:outer
    columns 1
    block:inner1
      a["one"] b["two"]
    end
    block:inner2
      c["three"]
    end
  end
  block:sibling
    d["four"]
  end
